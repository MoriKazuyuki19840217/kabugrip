# ====================== 株の握力測定アプリ 用 requirements.txt ======================
# 最終更新: 2026年3月
# Streamlit Community Cloud で安定動作・高速デプロイ向けに最適化

streamlit>=1.55.0          # 最新安定版（2026年3月リリースの1.55.0対応）
pandas>=2.2.0              # データフレーム処理の安定性重視
yfinance>=1.2.0            # 最新版（2026年2月リリース）。fast_info/history がより安定
plotly>=6.6.0              # Plotly Express の最新機能を使えるように（あなたのグラフで快適）

# ====================== コメント ======================
# - streamlit は最新版を強く推奨（1.55.0でタブやexpanderの挙動が改善）
# - yfinance を1.2.0以上に上げました。あなたの get_current_price 関数は1.xでも問題なく動作します（fast_infoが強化されています）
# - plotly==6.6.0 は現在の最新版なので、>=6.6.0 でOK
# - 必要最小限に抑えています。将来的にnumpyやrequestsが必要になったら解除してください

# ====================== 将来追加候補（今はコメントアウト） ======================
# numpy>=1.26.0
# requests>=2.32.0
